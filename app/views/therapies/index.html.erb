<body>
  <div id="example1" align="center">
    <p id="notice"><%= notice %></p>

    <div class="table-responsive">
      <br/>
      <h1>Terapias</h1>
      <div class="row justify-content-center">
      <%= link_to 'Cadastrar novo Funcionário', new_therapy_path, class: 'btn btn-success' %>
      </div>

        <%= content_tag :table, role: :datatable, data: { url: therapies_path(format: :json)} do %>
          <thead class="table">
            <tr>
              <th>Name</th>
              <th></th>
            </tr>
          </thead>

          <tbody class="table">
          </tbody>
        <% end %>
    </div>
  </div>
</body>
